# -*- coding: utf-8 -*-
# O usuário técnico (dz23_whatsapp_bot) é criado sob demanda em RUNTIME
# (quando o account já está carregado), não em tempo de load — por isso a
# migração não o cria aqui. Sem ação necessária neste upgrade.
def migrate(cr, version):
    return
